Modules needed:
1. pip install firebase_admin
2. pip install pycryptodome
3. pip install uuid6

Files needed (should be in the root directory):
1. firebase-key.json
2. firebasescrypt.py
3. hash-key.json

To run locally:
1. python server.py